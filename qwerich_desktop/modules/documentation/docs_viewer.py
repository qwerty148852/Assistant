"""
Documentation Viewer for Qwerich Desktop Application
Displays built-in documentation and help content
"""

import os


class DocumentationViewer:
    def __init__(self):
        self.docs_path = "docs"
        self.documents = self.load_documentation()
    
    def load_documentation(self):
        """Load documentation content"""
        # Create default documentation structure
        docs = {
            "about": {
                "title": "О Qwerich",
                "content": """
# О Qwerich

Qwerich - это кроссплатформенное десктоп-приложение с графическим интерфейсом, созданное для упрощения взаимодействия с системой и общения с виртуальным помощником.

## Философия проекта

Qwerich разработан с целью предоставить пользователю удобный и интуитивно понятный интерфейс для:
- Общения с виртуальным помощником
- Мониторинга состояния системы
- Обмена сообщениями в локальной сети
- Настройки персональных предпочтений

## Особенности

- Современный интерфейс в стиле Material Design
- Поддержка 5 цветовых тем
- Адаптивный дизайн
- Локализация на русском языке
- Поддержка работы в локальной сети
                """
            },
            "getting_started": {
                "title": "Начало работы",
                "content": """
# Начало работы

## Регистрация и вход

1. При первом запуске приложения вы увидите экран приветствия
2. Нажмите "Зарегистрироваться" для создания нового аккаунта
3. Введите свои данные и подтвердите, что вы не робот
4. После регистрации вы можете войти в систему

## Основные команды помощника

Qwerich реагирует на различные команды. Вот несколько примеров:

- **Приветствие**: "Привет", "Хай", "Здравствуй"
- **Информация о погоде**: "Какая погода?", "Что на улице?"
- **Состояние помощника**: "Как дела?", "Что ты умеешь?"
- **Прощание**: "Пока", "До встречи"

## Навигация по приложению

- Используйте боковое меню для перехода между разделами
- Настройки доступны в соответствующем разделе
- История чата сохраняется автоматически
                """
            },
            "messenger": {
                "title": "Мессенджер",
                "content": """
# Мессенджер

## Как работает локальная сеть

Qwerich включает встроенный мессенджер для общения внутри локальной сети. Приложение использует UDP-широковещание для обнаружения других экземпляров Qwerich в сети и TCP-соединения для обмена сообщениями.

## Подключение

1. Убедитесь, что все участники находятся в одной локальной сети
2. Приложение автоматически обнаружит других пользователей Qwerich
3. Выберите пользователя из списка онлайн-участников
4. Начните общение

## Устранение проблем

- Проверьте настройки брандмауэра
- Убедитесь, что порты 12344-12345 открыты
- Проверьте, что все устройства подключены к одной сети
                """
            },
            "system_monitor": {
                "title": "Системный монитор",
                "content": """
# Системный монитор

## Описание метрик

### ЦПУ (Процессор)
- **Загрузка (%)**: Процент использования процессора
- **Количество ядер**: Количество логических и физических ядер
- **Температура**: Темпера в процессора (если датчики доступны)
- **Частота**: Текущая и максимальная частота процессора

### Память (ОЗУ)
- **Общая память**: Общий объем установленной оперативной памяти
- **Использовано**: Объем используемой памяти
- **Доступно**: Объем свободной памяти
- **Процент использования**: Процент загрузки оперативной памяти

### Диски
- **Устройство**: Имя устройства хранения
- **Точка монтирования**: Путь к диску в файловой системе
- **Файловая система**: Тип файловой системы (NTFS, ext4 и т.д.)
- **Общий объем**: Общий объем диска
- **Использовано**: Использованное пространство
- **Свободно**: Свободное пространство

### Сеть
- **IP-адрес**: Адрес в локальной сети
- **Скорость передачи/приема**: Скорость обмена данными
                """
            },
            "settings": {
                "title": "Настройки",
                "content": """
# Настройки

## Внешний вид

### Цветовые темы
Приложение предоставляет 5 различных цветовых тем:
- **Deep Forest**: Темно-зеленая палитра
- **Ocean Blue**: Глубокий синий
- **Dark Amethyst**: Темно-фиолетовая
- **Light Breeze**: Светлая тема
- **True Black**: Абсолютно темная тема

### Анимации
- Переключатель для включения/отключения анимаций
- Отключение анимаций может улучшить производительность на слабых системах

## Учетная запись

- Изменение имени пользователя
- Изменение привязанного email
- Смена пароля

## Конфиденциальность

- Управление сбором анонимной статистики
- Настройки конфиденциальности

## Мессенджер

- Настройка порта для соединения
- Изменение никнейма в сети
- Управление звуковыми уведомлениями
                """
            }
        }
        
        return docs
    
    def get_document(self, doc_id):
        """Get a specific document by ID"""
        if doc_id in self.documents:
            return self.documents[doc_id]
        return None
    
    def get_all_documents(self):
        """Get list of all available documents"""
        return list(self.documents.keys())
    
    def get_document_by_title(self, title):
        """Get a document by its title"""
        for doc_id, doc in self.documents.items():
            if doc['title'] == title:
                return doc
        return None
    
    def search_documentation(self, query):
        """Search for documentation containing the query"""
        results = []
        query_lower = query.lower()
        
        for doc_id, doc in self.documents.items():
            if query_lower in doc['title'].lower() or query_lower in doc['content'].lower():
                results.append({
                    'id': doc_id,
                    'title': doc['title'],
                    'preview': doc['content'][:200] + "..."
                })
        
        return results


# Example usage
if __name__ == "__main__":
    docs = DocumentationViewer()
    
    print("Available documents:")
    for doc_id in docs.get_all_documents():
        doc = docs.get_document(doc_id)
        print(f"- {doc['title']} (ID: {doc_id})")
    
    print("\nSearching for 'настройки':")
    search_results = docs.search_documentation('настройки')
    for result in search_results:
        print(f"- {result['title']}: {result['preview']}")
    
    print("\nContent of 'about' document:")
    about_doc = docs.get_document('about')
    print(about_doc['content'])